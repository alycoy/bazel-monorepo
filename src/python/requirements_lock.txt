# Flask==3.0.0

# We need to specify every dependency that flask needs to run
# TODO: Check in rules_python documentation if we have a way to skip this step
click==8.1.3
Flask==2.0.2
itsdangerous==2.1.2
Jinja2==3.1.2
MarkupSafe==2.1.2
Werkzeug==2.3.4